[build]
  command = "bun install && bun run generate"
  publish = ".output/public"

[build.environment]
  BUN_VERSION = "1.0.3"

[[headers]]
  for = "/*"
  [headers.values]
    Cross-Origin-Embedder-Policy = "unsafe-none"
    Cross-Origin-Opener-Policy = "unsafe-none"
